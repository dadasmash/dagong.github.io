# Project instructions

For any request to update, synchronize, or rebuild this website from an academic CV, read and follow [skills/update-website-from-cv/SKILL.md](skills/update-website-from-cv/SKILL.md) before editing files.

The existing website is the design source of truth. Preserve its page layouts and formatting; use the CV only as the content source of truth. Ask the user before any uncertain classification, deletion, replacement, expansion, or invented content.

If an update requires a file or image that the user has not placed in the corresponding project folder, remind the user to attach or add it. Do not generate a replacement file or image unless the user explicitly instructs you to do so.
